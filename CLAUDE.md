# Expense Tracker

Vanilla JavaScript web application for tracking expenses.

## Structure

Single file: `index.html` with embedded CSS and JS. No build step, no frameworks.

## Dependencies

- **Chart.js** — loaded from CDN (used for expense chart visualization)

## Running

Open `index.html` in a browser. No server required.
